spring:
  datasource:
    username: [USUARIO]
    password: [SENHA]
    url: jdbc:postgresql://[ENDEREÇO_BANCO]:[PORTA]/[NOME_BANCO]
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format-sql: true
    show-sql: true
api:
  security:
    token:
      secret:
server:
  error:
    include-stacktrace: never
